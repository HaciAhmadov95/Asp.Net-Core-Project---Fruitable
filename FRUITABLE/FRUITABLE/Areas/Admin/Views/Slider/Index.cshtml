@model List<Slider>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <a class="btn btn-success" asp-area="Admin" asp-controller="Slider" asp-action="Create">Create</a>


            <table class="table table-bordered table-contextual mt-5">
                <thead>
                    <tr>
                        <th> Image </th>
                        <th> Name </th>
                        <th> Settings </th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>

                            <td> <img src="~/img/@item.Image"> </td>
                            <td> @item.SliderTitle </td>
                            <td>
                                <a class="btn btn-info" asp-area="Admin" asp-controller="Slider" asp-route-id="@item.Id" asp-action="Detail">Detail</a>
                                <a class="btn btn-primary" asp-area="Admin" asp-controller="Slider" asp-route-id="@item.Id" asp-action="Edit">Edit</a>
                                <a class="btn btn-danger" asp-area="Admin" asp-controller="Slider" asp-route-id="@item.Id" asp-action="Delete">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

